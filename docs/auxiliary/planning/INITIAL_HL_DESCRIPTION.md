# HL Description

CONTEXT:
I need to create a simple dockerized Java app. The app must have a backend, frontend, and a DB. It must use docker-compose to build and run. It must run on a machine in the cloud, in my case, AWS EC2. I want create the AWS infrastructure using Terraform (VPC, Subnets, SGs, IAM Roles, ALB, EC2, ASG, Launch Template, ECR, Secrets Manager, etc.). It must use AWS Terraform modules where possible. It must include Auto Scaling. The app GUI needs to be accessible from the world. I want to use GitHub Actions for CI/CD. The app must be scaled in such a way that it is automatically starts on the new machine and is accessible in the same way the original machine is. I want to use a central MySQL DB so that all the app instances talk to the same DB. There must be security best practices implemented. Performance best practices. A certain measure of observability (NOT A FULL BLOWN SYSTEM) using the appropriate AWS Services. We'll just need to create an A record for the new sub-domain (java.talorlik.com) - see EXISTING RESOURCES.
The Java app mustn't be too complicated. Perhaps a simple login/sign form with basic fields, simple email verification code step after signup, a thank you for signing up page, a simple profile page after login with ability to alter your details except your email, an admin page allowing the admin user to manage the registrants with all the usual elements, the admin user must be seeded into the DB upon deployment and his username and password must be written to AWS Secrets Manager for ease of access. The DB username and password must also be created and written to the AWS Secrets Manager. DB must employ best practices for performance, scaling, security. The Terraform state will be created and managed in the same account as the deployment account for simplicity. There must be unit tests, integration tests, e2e tests which must run as part of the CI/CD and must prevent deployment if they fail. The Java app and its Dockerfile must employ best practices. The Java app must be built using Gradle or Maven (whichever is simpler). The port to use to expose the app internally must be 8080 but externally on 8443 (HTTPS). The EC2 OS must be Ubuntu (the latest version available on AWS).

IMPORTANT: It must be built such that I can give the code plus instructions and they should be able to set it up on their account following those instructions. The instructions must include an explanation on setting up.

EXISTING RESOURCES: I have a domain (talorlik.com) which is in a different account (we'll refer to it as the DOMAIN account) to where all this is going to be deployed to (we'll refer to it as the DEPLOYMENT account). I have an ACM certificate issued in the deployment account. I have an existing role (github-role) to be used to deploy the resources with.

TASKS:

- Use your knowledge of some of the recent projects I've planned with you as well as online references to produce a bottom-up project plan which includes both theory and technical.
- It must start with the base infrastructure and follow with the application.
- Break the plan into phases and domain-level.
